package app.entities;

public class Student extends Person {

    public Student(String firstName, String lastName, String email, String residenceCity) {
        super(firstName, lastName, email, residenceCity);
    }
}
